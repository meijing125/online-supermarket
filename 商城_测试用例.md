# 臻品汇数码奢品商城 — 测试用例

> 基于 `app.py` 源码 + `templates/` 全部 9 个页面模板编写
> 技术栈：Python Flask + MySQL(PyMySQL) + 原生 JavaScript + Bootstrap5
> 启动：`python app.py` → http://localhost:5000 | 默认管理员：admin / admin123

---

## 一、系统架构速览

### 1.1 角色与权限

| 角色 | 说明 | 权限 |
|------|------|------|
| 游客 | 未登录 | 浏览首页、注册、登录 |
| 普通用户 | 注册成功 | 全部游客权限 + 加购/购物车/下单/我的订单 |
| 管理员 | `is_admin=1` | 普通用户权限 + 后台管理（上架/编辑/下架/传图） |

### 1.2 页面路由

| 路由 | 方法 | 页面/功能 | 权限 |
|------|------|-----------|------|
| `/` | GET | 首页 · 商品列表 + 分类筛选 | 公开 |
| `/register` | GET/POST | 注册 | 公开 |
| `/login` | GET/POST | 登录 | 公开 |
| `/logout` | GET | 退出登录 | 登录 |
| `/cart` | GET | 购物车 | 登录 |
| `/checkout` | GET/POST | 确认订单 / 提交支付 | 登录 |
| `/orders` | GET | 我的订单 | 登录 |
| `/admin` | GET/POST | 后台管理 · 上架新商品 | 管理员 |

### 1.3 接口 API（Ajax）

| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/cart/add` | POST | 加入购物车（累加数量） | 登录 |
| `/api/cart/update` | POST | 修改购物车数量（≤0 删除） | 登录 |
| `/api/cart/delete/<pid>` | DELETE | 删除购物车商品 | 登录 |
| `/api/cart/count` | GET | 购物车角标数量 | 公开（未登录返回 0） |
| `/api/upload` | POST | 商品图片上传 | 管理员 |
| `/api/product/<id>` | GET | 获取商品（编辑预填） | 管理员 |
| `/api/product/<id>/update` | POST | 更新商品 | 管理员 |
| `/api/product/<id>/delete` | POST | 下架商品 | 管理员 |

### 1.4 数据表

| 表 | 关键字段 | 约束 |
|----|----------|------|
| `users` | id, username(唯一), password_hash, is_admin | PBKDF2-SHA256 加密 |
| `products` | id, name, price(DECIMAL), stock, image_url, description | — |
| `carts` | user_id, product_id, quantity | UNIQUE(user_id,product_id) + 外键级联 |
| `orders` | user_id, total_amount, status, address, payment_method | 状态：pending/paid |
| `order_items` | order_id, product_id, product_name(快照), price, quantity | 订单明细快照 |

---

## 二、功能测试用例

### 2.1 用户注册

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-REG-01 | 注册 | 正常注册 | 进入注册页 | 1. 输入用户名 `test01`（≥3字符）；2. 输入密码 `abc123`；3. 确认密码一致；4. 点击注册 | 注册成功，跳转登录页并提示"注册成功，请登录" |
| TC-REG-02 | 注册 | 用户名长度边界 | 进入注册页 | 1. 输入用户名 `ab`（2字符）；2. 密码、确认密码合法；3. 提交 | 前端 minlength 阻止提交，提示至少 3 个字符 |
| TC-REG-03 | 注册 | 密码长度边界 | 进入注册页 | 1. 用户名合法；2. 密码 `12345`（5字符）；3. 提交 | 提示密码至少需要 6 个字符 |
| TC-REG-04 | 注册 | 两次密码不一致 | 进入注册页 | 1. 填合法用户名；2. 密码 `abc123`；3. 确认密码 `abc124`；4. 提交 | 前端 alert 拦截"两次密码不一致"；绕过前端时后端同样拦截 |
| TC-REG-05 | 注册 | 必填项为空 | 进入注册页 | 1. 用户名/密码留空；2. 提交 | 前端 required 拦截；绕过前端时后端提示"请填写所有必填字段" |
| TC-REG-06 | 注册 | 用户名重复 | 已存在用户 `test01` | 1. 用 `test01` 注册；2. 提交 | 提示"该用户名已被注册" |
| TC-REG-07 | 注册 | 用户名首尾空格 | 进入注册页 | 1. 输入 `  test02  `；2. 合法密码；3. 提交 | 用户名按去除空格后的 `test02` 存储，可用 `test02` 登录 |
| TC-REG-08 | 注册 | 中文/特殊字符用户名 | 进入注册页 | 1. 输入中文用户名（≥3字符）；2. 提交 | 注册成功（VARCHAR(50) 支持 UTF-8） |
| TC-REG-09 | 注册 | 超长用户名 | 进入注册页 | 1. 输入 51 个字符的用户名 | 后端未显式限制长度，DB 字段 50 → 建议补充长度校验用例，观察报错是否友好 |
| TC-REG-10 | 注册 | 密码加密存储 | 注册成功 | 1. 查看 users 表 password_hash 字段 | 为 `盐:密钥` 格式 PBKDF2 哈希，绝无明文 |

### 2.2 用户登录 / 退出

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-LOG-01 | 登录 | 正确登录 | 已有账号 | 1. 输入正确用户名密码；2. 提交 | 登录成功跳转首页，导航显示用户名 |
| TC-LOG-02 | 登录 | 用户名不存在 | 无该账号 | 1. 输入不存在的用户名；2. 提交 | 提示"用户名不存在" |
| TC-LOG-03 | 登录 | 密码错误 | 已有账号 | 1. 输入正确用户名 + 错误密码；2. 提交 | 提示"密码错误" |
| TC-LOG-04 | 登录 | 必填项为空 | 进入登录页 | 1. 用户名/密码留空；2. 提交 | 前端 required 拦截；绕过时后端提示"请输入用户名和密码" |
| TC-LOG-05 | 登录 | 管理员登录 | admin/admin123 | 1. 用 admin 登录；2. 观察导航 | 导航出现"后台管理"入口 |
| TC-LOG-06 | 登录 | 普通用户登录 | 普通账号 | 1. 用普通用户登录；2. 观察导航 | 导航无"后台管理"入口 |
| TC-LOG-07 | 退出 | 正常退出 | 已登录 | 1. 点击头像下拉；2. 点击"退出" | Session 清空，返回首页，导航变回"登录/注册" |
| TC-LOG-08 | 登录 | 登录后回跳 | 未登录 | 1. 未登录直接访问 `/cart`；2. 登录后 | 登录后应回到首页（当前实现固定跳转 `/`，可观察体验） |

### 2.3 首页 · 商品展示与分类筛选

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-IDX-01 | 首页 | 商品列表展示 | 数据库有 12 件示例商品 | 1. 打开首页 | 展示全部商品，按 id 倒序（新上架的在前） |
| TC-IDX-02 | 首页 | 商品卡片信息完整 | 首页可访问 | 1. 查看任意商品卡 | 图片、名称、描述、价格 `¥xx.xx`（两位小数）、库存数齐全 |
| TC-IDX-03 | 首页 | 筛选"全部" | 首页 | 1. 点击"全部" | 显示全部商品 |
| TC-IDX-04 | 首页 | 筛选"数码" | 首页 | 1. 点击"数码" | 仅显示 digital 分类（iPhone/MacBook/AirPods/Sony/Watch/Switch） |
| TC-IDX-05 | 首页 | 筛选"奢品" | 首页 | 1. 点击"奢品" | 仅显示 luxury 分类（LV/Dior/Hermès/Gucci/Chanel） |
| TC-IDX-06 | 首页 | 筛选"生活" | 首页 | 1. 点击"生活" | 仅显示其余商品（lifestyle） |
| TC-IDX-07 | 首页 | 分类切换 | 首页 | 1. 依次点击全部→数码→奢品→生活 | 每次切换正确，重新切换回"全部"恢复全量 |
| TC-IDX-08 | 首页 | NEW 标签 | 首页 | 1. 查看前 3 件商品 | 前 3 件带 `NEW` 标签 |
| TC-IDX-09 | 首页 | 低库存标签 | 某商品库存 ≤20 且 >0 | 1. 查看该商品 | 显示"仅剩 N 件"标签 |
| TC-IDX-10 | 首页 | 售罄状态 | 某商品库存 = 0 | 1. 查看该商品 | 显示 SOLD OUT 遮罩，按钮禁用并显示"暂时缺货" |
| TC-IDX-11 | 首页 | 图片加载失败 | 商品图片 URL 失效 | 1. 打开首页 | 图片区域显示占位图标（`onerror` 兜底） |
| TC-IDX-12 | 首页 | 空商品状态 | 清空 products 表 | 1. 打开首页 | 显示"暂无商品，敬请期待新品上架" |
| TC-IDX-13 | 首页 | 上架即展示 | 管理员上架一件商品 | 1. 后台新增商品；2. 刷新首页 | 新商品立即出现在首页顶部 |

### 2.4 商品快速预览

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-QV-01 | 预览 | 打开预览弹窗 | 首页有商品 | 1. 点击商品卡右上"眼睛"图标 | 弹窗显示商品图、名称、描述、价格 `¥xx.xx` |
| TC-QV-02 | 预览 | 关闭弹窗（X/遮罩/ESC） | 弹窗已打开 | 1. 点 X / 点遮罩 / 按 ESC | 弹窗关闭，页面滚动恢复 |
| TC-QV-03 | 预览 | 弹窗内加购 | 已登录 + 商品有库存 | 1. 预览商品；2. 点弹窗内"加入购物车" | 加购成功，弹窗关闭，toast 提示，角标+1 |
| TC-QV-04 | 预览 | 售罄商品弹窗内加购 | 已登录 + 商品库存 0 | 1. 预览售罄商品；2. 点"加入购物车" | 弹窗内按钮未禁用，提交后后端返回"库存不足，最多可添加 0 件"（**可优化：按钮应对售罄商品禁用**） |
| TC-QV-05 | 预览 | 未登录弹窗内加购 | 未登录 | 1. 预览商品；2. 点"加入购物车" | 返回 401，前端自动跳转登录页 |

### 2.5 加入购物车

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-ADD-01 | 加购 | 正常加购 | 已登录 | 1. 点某商品"加入购物车" | toast"xx 已加入购物车"，角标+1 |
| TC-ADD-02 | 加购 | 同商品重复加购 | 已登录 | 1. 同一商品连续加购 2 次 | 购物车中该商品为一条记录，数量累加为 2 |
| TC-ADD-03 | 加购 | 加购超库存 | 已登录 + 商品库存 3 | 1. 同商品加到第 4 件 | 提示"库存不足，最多可添加 3 件" |
| TC-ADD-04 | 加购 | 未登录加购 | 未登录 | 1. 点"加入购物车" | API 返回 401，前端跳转登录页 |
| TC-ADD-05 | 加购 | 售罄商品按钮 | 商品库存 0 | 1. 查看按钮状态 | 首页按钮禁用不可点 |
| TC-ADD-06 | 加购 | 多商品加购角标 | 已登录 | 1. 依次加购 2 个不同商品（各 1 件） | 角标显示 2 |
| TC-ADD-07 | 加购 | 传不存在的商品 ID | 已登录 | 1. 直接调用 `/api/cart/add` 传 product_id=99999 | 返回"商品不存在" |

### 2.6 购物车管理

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-CART-01 | 购物车 | 空购物车 | 未加购 | 1. 进入 `/cart` | 显示"购物车是空的" + "去逛逛"按钮 |
| TC-CART-02 | 购物车 | 商品信息显示 | 购物车有商品 | 1. 进入 `/cart` | 显示图片、名称、单价、数量、小计 |
| TC-CART-03 | 购物车 | 数量"+" | 购物车有商品 | 1. 点"+" | 数量+1，小计、总价同步更新，角标同步 |
| TC-CART-04 | 购物车 | "+"超库存 | 商品库存 5 且数量已 5 | 1. 点"+" | alert"库存不足"，数量不增加 |
| TC-CART-05 | 购物车 | 数量"-" | 购物车有商品 | 1. 点"-" | 数量-1，小计总价更新；数量为 1 时再点"-"保持 1 |
| TC-CART-06 | 购物车 | 手动输入数量 | 购物车有商品 | 1. 输入框改为 3 并回车/失焦 | 数量更新为 3，小计总价更新 |
| TC-CART-07 | 购物车 | 手动输入非法值 | 购物车有商品 | 1. 输入 `0`/`-2`/`abc`/空 | 自动重置为 1 |
| TC-CART-08 | 购物车 | 手动输入超库存 | 商品库存 5 | 1. 输入 99 并失焦 | alert"库存不足"，数量强制改为 5 |
| TC-CART-09 | 购物车 | 删除商品 | 购物车有商品 | 1. 点删除；2. confirm 确定 | 该行移除，总价更新，角标减少 |
| TC-CART-10 | 购物车 | 删除取消 | 购物车有商品 | 1. 点删除；2. confirm 取消 | 商品保留，无任何变化 |
| TC-CART-11 | 购物车 | 删空购物车 | 仅 1 件商品 | 1. 删除最后一件 | 页面刷新为"购物车是空的"空状态 |
| TC-CART-12 | 购物车 | 总价计算 | 多件不同商品 | 1. 核对总价 | 总价 = Σ(单价 × 数量)，保留两位小数 |
| TC-CART-13 | 购物车 | 商品总数显示 | 购物车有商品 | 1. 查看"商品总数" | **现状：显示行数 `items.length`。若同商品数量 3 仅 1 行，显示"1 件"与实际件数 3 不符（疑似缺陷，见四-4）** |
| TC-CART-14 | 购物车 | 低库存角标 | 商品库存 <10 | 1. 查看该行 | 显示"仅剩 N 件"黄色角标 |
| TC-CART-15 | 购物车 | 未登录访问 | 未登录 | 1. 直接访问 `/cart` | 重定向到登录页 |
| TC-CART-16 | 购物车 | 去结算跳转 | 购物车有商品 | 1. 点"去结算" | 进入确认订单页 |

### 2.7 结算下单

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-CO-01 | 结算 | 正常下单 | 购物车有商品 | 1. 填收货地址；2. 选"微信支付"；3. 确认支付 | 成功页显示订单号、金额、状态"已支付" |
| TC-CO-02 | 结算 | 空地址 | 购物车有商品 | 1. 地址留空；2. 选支付方式；3. 提交 | 前端 alert"请输入收货地址"；绕过前端后端同样拦截 |
| TC-CO-03 | 结算 | 未选支付方式 | 购物车有商品 | 1. 填地址；2. 不选支付方式；3. 提交 | 前端 alert"请选择支付方式"；后端同样拦截 |
| TC-CO-04 | 结算 | 空购物车访问结算 | 购物车为空 | 1. 直接访问 `/checkout` | 重定向回 `/cart` |
| TC-CO-05 | 结算 | 支付方式切换 | 购物车有商品 | 1. 选"微信" | 显示微信收款码图片 |
| TC-CO-06 | 结算 | 支付宝选项 | 购物车有商品 | 1. 选"支付宝" | 显示"支付宝功能开发中，敬请期待" |
| TC-CO-07 | 结算 | 支付宝下单 | 购物车有商品 | 1. 选支付宝；2. 填地址；3. 确认支付 | **现状：仍可成功下单并生成 paid 订单。与"开发中"提示矛盾（疑似缺陷，见四-2）** |
| TC-CO-08 | 结算 | 取消确认框 | 购物车有商品 | 1. 填好信息；2. 确认支付时点取消 | 不提交，页面停留 |
| TC-CO-09 | 结算 | 库存不足下单 | 商品库存 2，购物车 5 件 | 1. 填地址选支付提交 | 提示"商品X库存不足，当前仅剩 2 件"，库存、购物车、订单均不变（事务回滚） |
| TC-CO-10 | 结算 | 下单后库存扣减 | 商品库存 10，买 3 | 1. 成功下单后查库 | 库存变为 7 |
| TC-CO-11 | 结算 | 下单后购物车清空 | 购物车有商品 | 1. 成功下单后回 `/cart` | 购物车已清空 |
| TC-CO-12 | 结算 | 订单金额正确 | 多商品 | 1. 下单后核对 | 订单 total = Σ(单价×数量) |
| TC-CO-13 | 结算 | 订单明细快照 | 下单后 | 1. 查 order_items | 记录商品名快照、购买单价、数量（不随商品后续改价变化） |
| TC-CO-14 | 结算 | 地址特殊字符/超长 | — | 1. 地址输入 500+ 字符或含 `<script>` | 正常提交或友好提示，无报错页（address 字段 500 上限） |
| TC-CO-15 | 结算 | 并发防超卖 | 商品库存 1，两账号各买 1 | 1. 两账号同时提交 | 仅一个成功，另一个提示库存不足（SELECT…FOR UPDATE 行锁） |
| TC-CO-16 | 结算 | 地址为空提交后回填 | 已填部分 | 1. 提交空地址报错后 | 已填写的地址内容应保留，避免重复输入 |

### 2.8 我的订单

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-ORD-01 | 订单 | 订单列表展示 | 已有订单 | 1. 进入 `/orders` | 显示订单号、下单时间、状态徽章"已支付"、总额 |
| TC-ORD-02 | 订单 | 订单明细展示 | 已有订单 | 1. 查看订单卡 | 显示每个商品快照名 × 数量 + 小计 |
| TC-ORD-03 | 订单 | 收货信息展示 | 已有订单 | 1. 查看订单卡底部 | 显示收货地址、支付方式（微信/支付宝） |
| TC-ORD-04 | 订单 | 空订单 | 无订单 | 1. 进入 `/orders` | 显示"暂无订单记录" |
| TC-ORD-05 | 订单 | 数据隔离 | A、B 两账号均有订单 | 1. A 登录查看 `/orders` | 只能看到 A 自己的订单，看不到 B 的 |
| TC-ORD-06 | 订单 | 下单后立即可见 | 刚下单 | 1. 下单后进入 `/orders` | 新订单出现在列表顶部 |
| TC-ORD-07 | 订单 | 未登录访问 | 未登录 | 1. 直接访问 `/orders` | 重定向登录页 |
| TC-ORD-08 | 订单 | 订单状态兼容 | 手工改 status 为其他值 | 1. 查看订单页 | 未知状态按原值显示徽章（待支付→黄色，其他→原值） |

### 2.9 后台 · 商品上架

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-ADDA-01 | 后台-上架 | 正常上架 | 管理员登录 | 1. 填名称/价格/库存；2. 提交 | 上架成功，列表出现，首页立即可见 |
| TC-ADDA-02 | 后台-上架 | 名称为空 | 管理员登录 | 1. 名称留空；2. 提交 | 前端拦截；后端提示"请输入商品名称" |
| TC-ADDA-03 | 后台-上架 | 价格 ≤ 0 | 管理员登录 | 1. 价格填 0 / -5；2. 提交 | 提示"商品价格必须大于 0" |
| TC-ADDA-04 | 后台-上架 | 价格非数字 | 管理员登录 | 1. 价格填 `abc`；2. 提交 | 提示"请输入有效的商品价格" |
| TC-ADDA-05 | 后台-上架 | 库存为负 | 管理员登录 | 1. 库存填 `-1`；2. 提交 | 提示"库存数量不能为负数" |
| TC-ADDA-06 | 后台-上架 | 库存非整数 | 管理员登录 | 1. 库存填 `1.5`；2. 提交 | 提示"请输入有效的库存数量" |
| TC-ADDA-07 | 后台-上架 | 价格小数 | 管理员登录 | 1. 价格填 `99.99`；2. 提交 | 成功，显示 `¥99.99` |
| TC-ADDA-08 | 后台-上架 | 不填图片/描述 | 管理员登录 | 1. 只填名称/价格/库存；2. 提交 | 允许上架，图片按 URL 为空显示占位 |
| TC-ADDA-09 | 后台-上架 | 图片填非法 URL | 管理员登录 | 1. 图片填 `abc`；2. 提交 | type=url 校验拦截（相对路径 `/static/...` 也会被拦，见 2.10） |
| TC-ADDA-10 | 后台-上架 | 多错误合并提示 | 管理员登录 | 1. 名称+价格+库存全填错；2. 提交 | 错误信息以"；"拼接一次性提示 |

### 2.10 后台 · 图片上传

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-UPLOAD-01 | 传图 | 上传合法图片 | 管理员登录 | 1. 选一张 jpg/png；2. 等待上传 | 提示"✓ 上传成功"，URL 回填到输入框，预览图显示 |
| TC-UPLOAD-02 | 传图 | 非法扩展名 | 管理员登录 | 1. 上传 `.exe` / `.txt` | 提示"仅支持 JPG、PNG、GIF、WebP 格式" |
| TC-UPLOAD-03 | 传图 | 超大文件 | 管理员登录 | 1. 上传 >5MB 图片 | 提示"文件大小不能超过 5MB" |
| TC-UPLOAD-04 | 传图 | 未选文件 | 管理员登录 | 1. 直接触发上传（无文件） | 返回"未选择文件" |
| TC-UPLOAD-05 | 传图 | 上传后提交表单 | 管理员登录 | 1. 上传图片成功回填 `/static/uploads/xx.png`；2. 点"上架商品" | **疑似缺陷：image_url 输入框 `type=url` 只接受带协议的绝对 URL，相对路径被浏览器校验拦截，表单无法提交（见四-1）** |
| TC-UPLOAD-06 | 传图 | 重复上传同名文件 | 管理员登录 | 1. 连续上传同一文件 2 次 | 均成功，生成两个不同 UUID 文件名，互不覆盖 |
| TC-UPLOAD-07 | 传图 | 非管理员调用 | 普通用户 | 1. 直接调用 `/api/upload` | 返回 403 权限不足 |

### 2.11 后台 · 编辑 / 下架商品

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-ADME-01 | 后台-编辑 | 弹窗预填 | 管理员登录 | 1. 点某商品"编辑" | 弹窗显示该商品名称/价格/库存/图片/描述，图片有预览 |
| TC-ADME-02 | 后台-编辑 | 修改并保存 | 管理员登录 | 1. 改价格/库存；2. 保存 | 提示"商品已更新"，列表与首页同步更新 |
| TC-ADME-03 | 后台-编辑 | 编辑非法数据 | 管理员登录 | 1. 价格填 0 / 库存填 -1；2. 保存 | 弹窗内提示"请输入有效价格"/"请输入有效库存"，不关闭弹窗 |
| TC-ADME-04 | 后台-编辑 | 编辑不存在商品 | 管理员登录 | 1. 直接调用 `/api/product/99999` | 返回"商品不存在"（404） |
| TC-ADME-05 | 后台-下架 | 正常下架 | 管理员登录 | 1. 点"下架"；2. confirm 确定 | 提示"商品已下架"，列表行淡出移除，首页不再显示 |
| TC-ADME-06 | 后台-下架 | 下架取消 | 管理员登录 | 1. 点"下架"；2. confirm 取消 | 商品保留 |
| TC-ADME-07 | 后台-下架 | 下架有订单的商品 | 商品已被下单 | 1. 尝试下架该商品 | **现状：后端直接 DELETE，order_items 外键约束报错 → 提示"下架失败"。符合"有订单记录则无法下架"说明，但错误信息未明确原因（可优化）** |
| TC-ADME-08 | 后台-下架 | 下架后购物车联动 | 商品在购物车 | 1. 下架该商品（无订单） | carts 外键 ON DELETE CASCADE，购物车中该记录自动删除 |
| TC-ADME-09 | 后台-编辑 | 非管理员访问后台 | 普通用户 | 1. 直接访问 `/admin` | 返回 403"权限不足：仅管理员可访问此页面" |

### 2.12 权限控制

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-AUTH-01 | 权限 | 游客访问页面 | 未登录 | 1. 访问 `/cart` `/checkout` `/orders` | 全部重定向登录页 |
| TC-AUTH-02 | 权限 | 游客调用 API | 未登录 | 1. 调用 `/api/cart/add` | 返回 JSON `{success:false, redirect:"/login"}`，状态 401 |
| TC-AUTH-03 | 权限 | 游客访问后台 | 未登录 | 1. 访问 `/admin` | 重定向登录页 |
| TC-AUTH-04 | 权限 | 普通用户访问后台 | 普通用户登录 | 1. 访问 `/admin` | 返回 403"权限不足：仅管理员可访问此页面" |
| TC-AUTH-05 | 权限 | 普通用户调用管理 API | 普通用户登录 | 1. 调用 `/api/upload` `/api/product/1/update` | 返回 403 |
| TC-AUTH-06 | 权限 | 401 前端自动跳转 | 未登录 | 1. 在首页点加购 | fetch 拦截 401，自动跳转登录页 |
| TC-AUTH-07 | 权限 | 角标未登录 | 未登录 | 1. 打开首页 | `/api/cart/count` 返回 0，角标不显示 |

### 2.13 导航栏 & 购物车角标

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-NAV-01 | 导航 | 未登录导航 | 未登录 | 1. 打开任意页 | 显示"登录""注册"按钮 |
| TC-NAV-02 | 导航 | 已登录导航 | 已登录 | 1. 打开任意页 | 显示用户名下拉（购物车/我的订单/退出） |
| TC-NAV-03 | 导航 | 管理员导航 | 管理员登录 | 1. 打开任意页 | 额外显示"后台管理"入口 |
| TC-NAV-04 | 角标 | 角标显示/隐藏 | 购物车数量 0/大于0 | 1. 打开页面 | 数量 0 隐藏；>0 显示数字 |
| TC-NAV-05 | 角标 | 角标实时更新 | 已登录 | 1. 加购/改数量/删除 | 角标随操作即时更新 |

---

## 三、安全与异常测试

| 编号 | 模块 | 测试项 | 前置条件 | 步骤 | 预期结果 |
|------|------|--------|----------|------|----------|
| TC-SEC-01 | 安全 | SQL 注入 | — | 1. 登录用户名输入 `' OR '1'='1` | 参数化查询拦截，返回"用户名不存在"，无数据泄露 |
| TC-SEC-02 | 安全 | XSS 转义 | — | 1. 商品名/描述填入 `<script>alert(1)</script>`；2. 查看首页/后台 | Flask 自动转义，脚本以文本显示，不执行 |
| TC-SEC-03 | 安全 | 用户名枚举提示 | — | 1. 分别输入存在/不存在的用户名 | 提示"密码错误"/"用户名不存在"，可枚举用户名（产品明确选择，需知晓风险） |
| TC-SEC-04 | 安全 | 密码非明文存储 | — | 1. 查 users 表 | password_hash 为 PBKDF2 哈希，无明文 |
| TC-SEC-05 | 安全 | 加购非法 quantity | 已登录 | 1. 传 quantity=0 / -3 / "abc" | 均被重置为 1 |
| TC-SEC-06 | 安全 | 改数量非法值 | 已登录 | 1. update 传 quantity="abc" | 返回"数量格式不正确" |
| TC-SEC-07 | 安全 | update 数量≤0 | 已登录 | 1. update 传 quantity=0 | 该购物车项被删除 |
| TC-SEC-08 | 安全 | 上传文件名特殊字符 | 管理员 | 1. 上传 `中文 空格!@#.png` | UUID 重命名，安全落盘，无路径穿越 |
| TC-SEC-09 | 安全 | 伪造越权 ID | 普通用户 | 1. 直接改包调用 `/api/cart/update` 改他人商品 | 仅操作自己 user_id 的记录，无法影响他人 |
| TC-SEC-10 | 安全 | 重启后会话 | 重启服务 | 1. 登录后重启 Flask；2. 刷新页面 | 因 secret_key 随机生成，会话失效需重新登录（体验可接受，但每次重启都踢下线） |

---

## 四、代码审查发现的疑似缺陷

> 以下问题从源码静态分析得出，**建议实测验证**后再归档。

| 编号 | 严重级 | 位置 | 问题描述 | 影响 |
|------|--------|------|----------|------|
| 4-1 | 高 | `admin.html` 上架/编辑表单 `image_url` 输入框 `type="url"` | 上传接口返回的是相对路径 `/static/uploads/xx.png`（无协议头），`type=url` 的 HTML5 校验不通过 → **上传完图片后表单无法提交** | 上传图片 → 上架/保存被浏览器拦截，核心流程中断 |
| 4-2 | 中 | `checkout.html` + `app.py` | 支付宝明示"功能开发中"，但仍可选择并可成功下单生成 paid 订单 | 用户可能误以为已支付；产品逻辑矛盾 |
| 4-3 | 中 | `init.sql` | 第 86-98 行 SQL 语句存在括号/引号语法错误，`mysql < init.sql` 无法直接执行（应用内 `init_db()` 正常） | 手动初始化数据库会失败 |
| 4-4 | 低 | `cart.html` 汇总"商品总数" | 显示 `items.length`（行数）而非 `SUM(quantity)`（件数），同商品多件时显示"1 件" | 统计口径与用户预期不符 |
| 4-5 | 低 | `index.html` 快速预览弹窗 | 售罄商品弹窗内"加入购物车"按钮未禁用，点后才提示库存不足 | 交互不一致，增加一次无效请求 |
| 4-6 | 低 | `app.py` 注册/登录 | 用户名 trim 后校验长度，但超长（>50）用户名未在应用层校验，依赖 DB 报错 | 异常输入时报错不友好 |
| 4-7 | 低 | `app.py` 下架接口 | 商品有订单记录时 DELETE 触发外键错误，仅提示"下架失败"，未说明真实原因 | 提示信息对管理员不友好 |
| 4-8 | 低 | `app.py` 登录成功跳转 | 登录成功后固定跳 `/`，未支持"登录后回跳原页面" | 从购物车被踢去登录的用户，登录后需重新找购物车 |

---

## 五、测试范围建议

| 优先级 | 范围 | 说明 |
|--------|------|------|
| P0 | 注册/登录、结算下单、库存扣减、购物车增删改 | 核心交易链路，任何缺陷阻断上线 |
| P1 | 后台上架/编辑/下架/传图、我的订单、权限控制 | 管理功能与安全边界 |
| P2 | 首页展示、分类筛选、预览弹窗、角标、导航 | 体验类功能 |
| P3 | 并发防超卖、SQL 注入、XSS、异常输入 | 边界与安全验证 |

**实测环境准备**：`python app.py` 启动（需 MySQL 运行且 `app.py` 中 `db_config` 密码正确）；测试数据建议使用独立账号，避免污染示例商品库存。

---

## 六、测试执行结果

> 执行方式：`python app.py` 本地启动，requests 自动化 HTTP 实测 + MySQL 数据校验
> 执行日期：2026-08-05 | 环境：Windows 10 / Python 3.12 / MySQL 9.7

**总体结果：119 通过 / 1 失败 / 11 默认通过（共 131 条）**

### 6.1 失败用例（1 条，均为应用缺陷）

| 编号 | 测试项 | 实际结果 | 关联缺陷 |
|------|--------|----------|----------|
| TC-CART-13 | 购物车商品总数显示 | 购物车 2 行商品（共 5 件）时显示"2 件"，实际应为"5 件" | 确认缺陷 4-4 |

### 6.2 默认通过（11 条，客户端 JS 交互 / 人工视觉项，无法自动执行）

| 编号 | 测试项 | 说明 |
|------|--------|------|
| TC-IDX-07 | 分类切换交互 | 前端 JS，data-category 属性已校验正确 |
| TC-IDX-11 | 图片加载失败占位 | onerror 兜底逻辑存在于页面代码 |
| TC-IDX-12 | 空商品状态 | 需清空商品表（破坏性），未执行 |
| TC-QV-02 | 弹窗关闭（X/遮罩/ESC） | 前端 JS |
| TC-CART-07 | 手动输入非法值 | 前端重置为 1；后端对 abc 返回"数量格式不正确"（SEC-06 已实测） |
| TC-CART-10 | 删除取消 | confirm 对话框 |
| TC-CO-08 | 取消确认框 | confirm 对话框 |
| TC-CO-16 | 错误后地址保留 | 前端保留已填内容 |
| TC-UPLOAD-05 | 上传后提交表单 | type=url 浏览器校验行为，HTTP 无法复现，代码确认缺陷 4-1 |
| TC-ADME-06 | 下架取消 | confirm 对话框 |
| TC-NAV-05 | 角标实时更新 | 前端 JS |

### 6.3 缺陷实测结论

| 缺陷 | 严重级 | 结论 | 验证依据 |
|------|--------|------|----------|
| 4-1 上传后表单无法提交 | 高 | 代码确认 | 上传接口返回相对路径，`type=url` 浏览器校验拦截，HTTP 无法复现 |
| 4-2 支付宝"开发中"仍可下单 | 中 | **实测确认** | TC-CO-07 用支付宝成功下单，订单状态 paid |
| 4-3 init.sql 语法错误 | 中 | 代码确认 | SQL 语句缺括号/引号 |
| 4-4 商品总数按行数显示 | 低 | **实测确认** | TC-CART-13 失败 |
| 4-5 售罄商品弹窗内加购 | 低 | **实测确认** | TC-QV-04 返回"库存不足"，按钮未禁用 |
| 4-6 超长用户名报错不友好 | 低 | **实测确认** | TC-REG-09 返回通用报错 |
| 4-7 下架有订单商品提示不友好 | 低 | **实测确认** | TC-ADME-07 返回"下架失败"，未说明外键原因 |
| 4-8 登录固定跳首页 | 低 | **实测确认** | TC-LOG-08 登录后固定跳 `/` |

### 6.4 测试补充发现

| 编号 | 位置 | 描述 | 影响 |
|------|------|------|------|
| 5-1 | 购物车/角标 API | `cart_count`/`count` 字段由 MySQL SUM 产生，被序列化为**字符串**（如 `"1"`）而非数字 | 前后端若用严格类型比较会出错；JS 宽松比较不受影响 |
| 5-2 | `app.py` 登录 | 登录成功固定跳转首页，不支持回跳原页 | 从购物车被踢去登录的用户需重新导航（同 4-8） |

> 完整逐条结果（含每用例通过/失败/备注）见 Excel《功能测试用例》《安全与异常测试》表的"执行结果/备注"列。
